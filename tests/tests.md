# Tests

Esta carpeta contiene las pruebas automatizadas del proyecto.

## TP4 - Calidad y Testing

Las pruebas unitarias reales estan en:

- `tests/simulator.test.ts`

Comandos:

```bash
npm test
npm run test:coverage
```

El foco esta en logica de negocio del simulador: calculos, saturacion, recomendaciones y reglas de conexion. No se testea UI.
